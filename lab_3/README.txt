Пока не сделано
нужно понять как работают пакеты

Чтобы использовать поля из другого пакета. Поле должно начинаться с заглавной буквы
это имитация ключевых слов private и public


цикл по массиву происходит так, где i - индекс элемента, el - элемент с этим индексом
	for i, el := range array {
		fmt.Println(i, el)
	}
так как в го работа есть со значениями и ссылками, то чтобы изменять массив в цикле, нужно сделать так
	тут мы передаем ссылку на массив, но el всё равно явл. отдельной переменной, а не частью массива
  поэтому массиву нужно передать созданное значение
  for i, el := range &array {
		el = rand.Intn(100)
		array[i] = el
	}
мне такое не по душе

Срез объявляется так же как массив но без указания длины в кв. скобках
var array []int

добавление элемента в срез
пофакту создание нового среза с новым значением
в других языках эта операция выполняется для нединамических массивов...
slice = append(slice, 100)

удаление элемента из среза
тут мы в ячейку среза помещает все числа до определенного индекса и всё что после этого индекса, при этом не включая индекс
slice = append(slice[:third_el], slice[third_el+1:]...)
мне такое не по душе